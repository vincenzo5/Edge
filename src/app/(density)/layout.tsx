import DensityModuleLayout from "@/app/components/home/DensityModuleLayout";

export default function DensityLayout({ children }: { children: React.ReactNode }) {
  return <DensityModuleLayout>{children}</DensityModuleLayout>;
}
